// Steg 1
using System.Runtime.InteropServices;

void Hello32()
{
    for (int i = 0; i < 32; i++)
    {
        System.Console.WriteLine("Hello World!");
    }
}

// Steg 2

void Integer(int kvadrat)
{
    Console.WriteLine(kvadrat * kvadrat);
}

// Steg 3

float Multi(float float1, float float2)
{
    float product = float1 * float2;
    return (product);
}

// Steg 4

float RightTriangleArea(float hight, float width)
{
    float area = (hight * width) / 2;
    return (area);
}

// Steg 5

double CircleArea(double radius)
{
    double area = (radius * radius) * Math.PI;
    return (area);
}

// Steg 6

void GetNumber()
{
string awnser = "";
while (true)
{
    
}
}